# v0.1.5 — Feature Complete Dashboard

## Added
- **User authentication system**: Register, login, JWT-based session with localStorage persistence
- **Auth middleware**: API routes protected behind Bearer token verification
- **Admin role**: `is_admin` flag on User model, admin-only endpoints
- **Admin panel**: User management (list, toggle admin, delete) at `/admin/users`
- **Password change**: `PATCH /auth/password` with old password verification
- **Landing page redesign**: Full marketing hero with gradient, feature cards, how-it-works, security section, tech stack, CTA
- **Pricing page**: Self-hosted vs Cloud options, comparison table vs Tailscale/Headscale/Netbird
- **Technology pages**: Four dedicated pages explaining WireGuard, Mesh VPN, ZTNA, and all encryption technologies used
- **Documentation page**: `/docs` replaces `/guide` with automatic redirect, improved content
- **Dropdown navbar**: Technology menu, Admin menu, Settings menu with hover dropdowns
- **Footer with contacts**: Email (nafiurohman@nlbs.me, nafiurohman@proton.me) and WhatsApp (+62 813-5819-8565)
- **Sync script**: `scripts/push-public.sh` to publish `client/` + `docs/` + `scripts/` to `github.com/nlbs/bnetscale`
- **Public README**: Full README for GitHub repo with install instructions

## Fixed
- **CodeBlock component**: Code content now renders properly (was using empty `<slot />` instead of `{{ code }}`)
- **Guide → Docs redirect**: Legacy `/guide` path redirects to `/docs`

## Changed
- Module path: `github.com/bezn/bnetscale` → `git.nafi-labs.tech/Labs/bnetscale`
- Navbar: auth-aware (shows login/register when logged out, email+logout when logged in)
- Install scripts URL references updated for GitHub public repo
- Version bumped to `v0.1.5`
